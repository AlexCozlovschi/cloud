apiVersion: v1
kind: Service
metadata:
  name: cloudappdeploy
  namespace: namespace-workflow-1716641034975
spec:
  type: LoadBalancer
  selector:
    app: cloudappdeploy
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000